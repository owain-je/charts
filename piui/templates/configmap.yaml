apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: {{ template "piui.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    component: {{ template "piui.fullname" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: configmap-{{ template "piui.name" . }}
data:
  appsettings.json: |+ 
    ﻿{
      "pi_app_service": "{{ .Values.pi_app_protocol }}{{ .Release.Namespace }}-{{ .Values.pi_app_service }}:{{ .Values.pi_app_port }}"
    }
