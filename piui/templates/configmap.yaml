apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: {{ template "name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    component: {{ template "fullname" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: configmap-{{ template "name" . }}
data:
  appsettings.json: |+ 
    ﻿{
      "pi_app_service": "{{ .Values.pi_app_protocol }}.{{ .Release.Namespace }}-{{ .Values.pi_app_service }}:{{ .Values.pi_app_port }}"
    }
